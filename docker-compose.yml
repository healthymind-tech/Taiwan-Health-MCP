version: '3.8'

services:
  app:
    build: .
    container_name: taiwanHealthMcp
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
